{
  "name": "dimension",
  "version": "0.1.0",
  "description": "A server system.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "webpack-dev-server --inline --progress --config ./client/build/webpack.dev.conf.js",
    "server": "pm2 start ./server/app.js --name my-node-api",
    "dev": "npm run server && npm run client"
    
  },
  "author": "saint jay",
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {
    "webpack-dev-server": "^2.9.1",
    "nodemon": "^1.17.5",
    "pm2": "^2.10.4"
  }
}
