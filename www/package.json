{
  "name": "dimension",
  "version": "0.1.0",
  "description": "A server system.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon ./server/app.js",
    "start": "pm2 start ./server/app.js --name my-node-api"
    
  },
  "author": "saint jay",
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {
  }
}
